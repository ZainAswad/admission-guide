# دليل إنشاء متجر جديد من هذا القالب

هذا الملف يشرح **كل** ما يجب تغييره لتحويل هذا المستودع إلى متجر لمحل آخر.
النظام والآلية تبقى كما هي: نفس لوحة التحكم، نفس آلية الطلبات عبر واتساب، نفس النشر إلى GitHub.

---

## ١) ألوان الهوية — `assets/css/styles.css`

كل الألوان مشتقّة من متغيّرات في أعلى الملف داخل `:root`. غيّر هذه السبعة فقط ويتغيّر الموقع كله:

```css
--brand:#0EBCBB;        /* اللون الأساسي (من الشعار) */
--brand-600:#0AA3A2;    /* أغمق قليلاً — للتحويم */
--brand-700:#088887;    /* أغمق — للنصوص على خلفية فاتحة */
--brand-800:#066B6A;    /* الأغمق — للأسعار والعناوين */
--brand-100:#D6F6F6;    /* فاتح — خلفيات الشارات */
--brand-50:#EFFBFB;     /* الأفتح — خلفيات البطاقات */
--brand-glow:rgba(14,188,187,.28);  /* هالة الأزرار — نفس اللون بشفافية */
```

**طريقة الاشتقاق:** خذ لون الشعار كـ `--brand`، ثم أغمقه ١٥٪ و٣٠٪ و٤٥٪ للثلاثة التالية،
وافتحه إلى ٩٠٪ و٩٦٪ للفاتحين. `--brand-glow` هو نفس اللون بشفافية 0.28.

الرصاصي والأبيض (`--ink` و`--grey` و`--surface`) يبقيان كما هما عادةً، إلا إن أراد المحل هوية مختلفة كلياً.

كذلك حدّث `<meta name="theme-color">` في `index.html` و`admin.html` ليطابق `--brand`.

## ٢) الشعار والصور — `assets/img/`

| الملف | المقاس | الاستخدام |
|---|---|---|
| `logo.png` | ~384 عرضاً | الصفحة الرئيسية وشاشة قفل اللوحة |
| `logo-sm.png` | ~128 عرضاً | الشريط العلوي والفوتر |
| `favicon.png` | 64×64 مربّع | أيقونة التبويب |
| `share.jpg` | 1200×630 | معاينة المشاركة في واتساب وفيسبوك |

`share.jpg` تُبنى من الشعار على خلفية بلون الهوية.
احذف محتويات `assets/img/products/` و`assets/img/brands/` (مع إبقاء `.gitkeep`).

## ٣) بيانات المتجر — `assets/js/data.js`

كل شيء داخل `SITE` في أعلى الملف:

- `name` `shortName` `nameEn` `tagline` `about`
- `siteUrl` — بعد أول نشر
- `phones` `whatsapp` — **أهم حقل: رقم واتساب هو الذي تصله الطلبات**
- `address` `city` `geo` (إحداثيات الخريطة) `hours` `social`
- `orders` — أجور التوصيل وحد التوصيل المجاني وبادئة رقم الطلب `prefix`
- `governorates` — تبقى كما هي للعراق
- `admin.hash` — **بصمة كلمة سر جديدة** (تُغيَّر من داخل اللوحة بعد أول دخول)
- `admin.repo` — `owner/repo` للمستودع الجديد

ثم `CATEGORIES` و`BRANDS` و`PRODUCTS` حسب نشاط المحل الجديد.

## ٤) نصوص الصفحات — `index.html`

- `<title>` و`<meta name="description">`
- `<meta name="theme-color">`
- `<link rel="canonical">` ووسوم `og:` و`twitter:` — **روابط مطلقة** للنطاق الجديد
- الشريط العلوي: `<strong>` و`<span>` داخل `.brand-txt`
- شريط الإعلان `.announce`
- الفوتر: الاسم، التاك لاين، النبذة، رقم الهاتف، رابطا `wa.me`
- الزر العائم: رابط `wa.me`

`admin.html` لا يحتاج تعديل نصوص — اسم المتجر يُقرأ من الإعدادات تلقائياً.

## ٥) الرسوم التوضيحية — `assets/js/icons.js`

الكائن `ART` فيه ٣٦ رسمة لمواد كهربائية. إن كان المحل الجديد بنشاط مختلف
(ملابس، عطور، قرطاسية…) فتحتاج مجموعة رسمات جديدة تناسبه، أو الاعتماد على الصور الحقيقية فقط.
مفاتيح `UI` (أيقونات الواجهة والتواصل) تبقى كما هي دائماً.

## ملفات لا تحتاج أي تعديل

`assets/js/orders.js` و`assets/js/store.js` و`assets/js/app.js` و`assets/js/admin.js`
و`vercel.json` و`_headers` — كلها عامة وتعمل مع أي متجر كما هي.
التخصيص كله في `data.js` و`styles.css` والصور ونصوص `index.html`.

## ٦) نظام الطلبات

`SITE.firebase` يحتاج **مشروع Firebase جديداً خاصاً بالمحل الجديد** — لا تشارك نفس المشروع
بين متجرين وإلا اختلطت طلبات المحلّين. اتبع `FIREBASE.md` من جديد بمشروع وحساب مدير مستقلّين.

## ٧) الاستضافة

`vercel.json` و`_headers` يعملان كما هما بلا تعديل.

---

## التمييز البصري بين المتجرين

اللون وحده لا يكفي ليبدو المتجران مختلفين. هذه المفاتيح تعطي شخصية مختلفة بلا مساس بالآلية:

| ما يتغيّر | أين | مثال |
|---|---|---|
| سلّم أنصاف الأقطار | `:root` → `--r-*` | حادّ (6px) بدل الدائري (16px) |
| الخط | `index.html` → رابط Google Fonts + `body{font-family}` | Tajawal أو Almarai بدل Cairo |
| شكل الأزرار | `.btn{border-radius}` | مستطيل بزوايا خفيفة بدل الكبسولة |
| ترتيب البنتو | `.bt1`…`.bt8` → `grid-area` | توزيع مختلف تماماً |
| بطاقة المنتج | `.card` | صورة أعرض، أو سعر فوق الاسم، أو إطار بدل الظل |
| الترويسة | `.topbar` | لون داكن كامل بدل الأبيض الشفاف |
| شدّة الظلال | `--e1`…`--e4` | مسطّح بحدود واضحة بدل الظلال |

---

## ترتيب العمل الموصى به

1. أنشئ المستودع الجديد وانسخ إليه ملفات هذا المستودع.
2. غيّر الألوان والشعار أولاً — يظهر أثر التغيير فوراً.
3. عدّل `SITE` بالكامل، وأفرغ `PRODUCTS`، واضبط `CATEGORIES` و`BRANDS`.
4. طبّق تغييرين أو ثلاثة من جدول التمييز البصري.
5. انشر على الاستضافة، ثم حدّث `siteUrl` والروابط المطلقة.
6. غيّر كلمة سر اللوحة من داخلها، ووصّلها بمفتاح GitHub للمستودع الجديد.
7. أدخل المواد الحقيقية من اللوحة.
