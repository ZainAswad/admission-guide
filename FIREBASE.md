# تفعيل نظام الطلبات

بدون هذه الخطوات يعمل المتجر بآلية واتساب كما كان. بعدها تصل الطلبات **داخل لوحة التحكم**
وتتحكم بقبولها ورفضها والتعليق عليها، ويرى الزبون حالة طلبه لحظياً.

الإعداد لمرة واحدة، ويستغرق حوالي عشر دقائق. الخطة المجانية تكفي متجراً صغيراً بمراحل.

---

## ١) إنشاء المشروع

1. افتح [console.firebase.google.com](https://console.firebase.google.com) وسجّل دخولك بحساب Google.
2. **Add project** ← اكتب اسماً (مثل `alwazani-store`) ← يمكنك إيقاف Google Analytics ← **Create**.

## ٢) قاعدة البيانات

1. من القائمة الجانبية: **Build ← Firestore Database ← Create database**.
2. اختر **Production mode** (سنضع القواعد بأنفسنا بعد قليل).
3. اختر أقرب موقع جغرافي — `eur3` أو `europe-west` مناسب للعراق.

## ٣) حساب المدير

1. **Build ← Authentication ← Get started**.
2. من **Sign-in method** فعّل **Email/Password** ← **Save**.
3. من تبويب **Users** ← **Add user** ← ضع بريدك وكلمة سر قوية ← **Add user**.

> هذا الحساب هو الذي تدخل به إلى تبويب «الطلبات» في لوحة التحكم. لا تشاركه مع أحد.

## ٤) قواعد الحماية — **الخطوة الأهم**

من **Firestore Database ← Rules**، امسح الموجود والصق التالي، وغيّر
`ضع-بريد-المدير-هنا` إلى بريد الحساب الذي أنشأته، ثم **Publish**:

```
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    function isAdmin() {
      return request.auth != null
          && request.auth.token.email == 'ضع-بريد-المدير-هنا';
    }

    match /orders/{orderId} {
      // الزبون يقرأ طلبه فقط إذا كان يملك معرّفه السري (رابط طلبه)
      allow get: if true;
      // لا أحد يستطيع تصفّح كل الطلبات أو تعديلها إلا المدير
      allow list, update, delete: if isAdmin();

      // إنشاء طلب جديد: متاح للزبائن بشروط تمنع العبث
      allow create: if request.auth == null
        && request.resource.data.status == 'pending'
        && request.resource.data.adminNote == ''
        && request.resource.data.total is number
        && request.resource.data.total >= 0
        && request.resource.data.total <= 100000000
        && request.resource.data.items.size() > 0
        && request.resource.data.items.size() <= 100
        && request.resource.data.customer.name is string
        && request.resource.data.customer.name.size() >= 2
        && request.resource.data.customer.name.size() <= 80
        && request.resource.data.customer.phone is string
        && request.resource.data.customer.phone.size() <= 20;
    }

    // كل ما عداه ممنوع
    match /{document=**} { allow read, write: if false; }
  }
}
```

**ماذا تفعل هذه القواعد؟**

- الزبون يستطيع **إرسال** طلب، ولا يستطيع تعديله بعد الإرسال ولا الاطّلاع على طلبات غيره.
- بيانات الزبائن (الأسماء والأرقام والعناوين) **لا يمكن تصفّحها** إلا من حسابك أنت.
- الزبون يتابع طلبه فقط عبر معرّفه السري المحفوظ في متصفحه.
- الطلب يُنشأ دائماً بحالة «بانتظار المراجعة» وبتعليق فارغ، فلا يستطيع أحد تزوير حالة مقبولة.

## ٥) ربط المتجر

1. في Firebase: **⚙ Project settings ← General**، وانزل إلى **Your apps**.
2. اضغط أيقونة الويب **`</>`** ← سمِّ التطبيق ← **Register app**.
3. انسخ قيمتين فقط من الشيفرة الظاهرة: **`apiKey`** و **`projectId`**.
4. في لوحة تحكم متجرك: **إعدادات المحل ← نظام الطلبات (Firebase)** ← الصق القيمتين وبريد المدير ← **حفظ الإعدادات**.
5. **انشر التغييرات** من تبويب النشر.

> `apiKey` ليس سرّاً — يظهر في كل موقع يستخدم Firebase، والحماية كلها في القواعد أعلاه.
> **السر الحقيقي هو كلمة سر حساب المدير.**

## ٦) التجربة

1. افتح المتجر، أضف مادة، أكمل الطلب.
2. افتح لوحة التحكم ← تبويب **الطلبات** ← سجّل دخولك بحساب المدير.
3. يجب أن يظهر الطلب. جرّب **قبول** ثم افتح **التفاصيل** وأضف تعليقاً وغيّر الحالة.
4. ارجع إلى صفحة الطلب عند الزبون — يرى الحالة الجديدة والتعليق.

---

## كيف تعرف بوصول طلب جديد؟

تبويب «الطلبات» يحمل **شارة حمراء** بعدد الطلبات الجديدة، ويحدّث نفسه كل ٣٠ ثانية ما دام مفتوحاً.
افتح اللوحة على هاتفك واتركها في تبويب، أو راجعها بين حين وآخر.

للتنبيه الفوري يمكن لاحقاً إضافة إشعار عبر Cloud Functions أو ربط بوت تيليكرام — أخبرني إن أردت ذلك.

## الحدود المجانية

الخطة المجانية (Spark) تعطي عشرات آلاف عمليات القراءة والكتابة يومياً — أكثر بكثير مما يحتاجه
متجر في مراحله الأولى. إن اقتربت من الحد يخبرك Firebase، وعندها تُرقّي الخطة أو ننقل التخزين.

## استكشاف الأعطال

| ما يظهر | السبب والحل |
|---|---|
| «الصلاحيات لا تسمح بهذه العملية» عند إرسال طلب | القواعد لم تُنشر، أو `create` غير مطابق. أعد لصق القواعد واضغط Publish |
| «الصلاحيات لا تسمح» عند عرض الطلبات | البريد في `isAdmin()` لا يطابق بريد الحساب الذي سجّلت به حرفياً |
| «البريد أو كلمة السر غير صحيحة» | راجع الحساب في Authentication ← Users |
| «نظام الطلبات غير مفعّل» | `apiKey` أو `projectId` فارغ في الإعدادات، أو لم تنشر بعد حفظها |
